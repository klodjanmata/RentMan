import React from 'react';
import { Typography, Box } from '@mui/material';

export const DefectsPage: React.FC = () => {
  return (
    <Box sx={{ textAlign: 'center', py: 4 }}>
      <Typography variant="h4">Defects Page</Typography>
      <Typography variant="body1">Vehicle defects management will be implemented here</Typography>
    </Box>
  );
};
