import React from 'react';
import { Typography, Box } from '@mui/material';

export const EmployeeManagementPage: React.FC = () => {
  return (
    <Box sx={{ textAlign: 'center', py: 4 }}>
      <Typography variant="h4">Employee Management Page</Typography>
      <Typography variant="body1">Employee management will be implemented here</Typography>
    </Box>
  );
};
