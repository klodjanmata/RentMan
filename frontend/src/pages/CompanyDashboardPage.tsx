import React from 'react';
import { Typography, Box } from '@mui/material';

export const CompanyDashboardPage: React.FC = () => {
  return (
    <Box sx={{ textAlign: 'center', py: 4 }}>
      <Typography variant="h4">Company Dashboard Page</Typography>
      <Typography variant="body1">Company management dashboard will be implemented here</Typography>
    </Box>
  );
};
